import { StyleSheet, Text, View } from "react-native";

const Try = () => {
   return (
      <View>
         <Text>Try</Text>
      </View>
   );
};

export default Try;

const styles = StyleSheet.create({});
