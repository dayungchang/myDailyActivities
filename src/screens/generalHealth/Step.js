import { StyleSheet, Text, View } from "react-native";

const Step = () => {
   return (
      <View>
         <Text>Step</Text>
      </View>
   );
};

export default Step;

const styles = StyleSheet.create({});

